sqlr -c local-postgresql -q / --query "select top 100 * from Users"
sqlr -c local-postgresql -i input.sql -o output.json

sqlr describe -c local-postgresql or sql describe -s "localhost:5432@user-db:pass123"

+----------+
| users    |
|----------|
| id (int) |
| name (s) |
+----------+

sqlr describe -c local-postgresql --compact

dbo.users [id int, name? varchar(250)]
dbo.books [id int, name? varchar(250), user_id fk("dbo.users.id")]
...

sqlr describe -c local-postgresql --json

sqlr connections --add 
  Select type of connection:
  [*] PostgreSQL
  [ ] Microsoft SQL Server
  [ ] MySQL
  Provide name of connection: local-postgresql
  Provide connection string: "localhost:5432@user-db:pass123"
  Do you want to encrypt connection? (Use for PROD databases)
    yes -> Provide password to secure connection string
    no

sqlr connections

Available connections:

* local-postgresql
* docker-local
* prod-postgresql

sqlr connections --remove -c local-postgresql
sqlr connections --show -c local-postgresql

