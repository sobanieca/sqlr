{
  "compilerOptions": {
    "checkJs": true,
    "noImplicitAny": false,
    "strictNullChecks": false
  },
  "lint": {
    "rules": {
      "exclude": ["no-import-prefix"]
    }
  },
  "tasks": {
    "test": "deno test -A test_*.js",
    "update-snapshots": "deno test -A test_*.js -- --update"
  }
}
